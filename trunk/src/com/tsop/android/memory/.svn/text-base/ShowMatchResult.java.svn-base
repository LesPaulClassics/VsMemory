package com.tsop.android.memory;

import com.tsop.android.memory.R;
import com.tsop.android.memory.R.layout;
import com.tsop.games.memory.IShowMatchResult;

import android.app.Activity;
import android.os.Bundle;

public class ShowMatchResult extends Activity implements IShowMatchResult{
    /** Called when the activity is first created. */
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.match_result);
    }
}